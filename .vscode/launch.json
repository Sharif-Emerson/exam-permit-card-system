{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Exam Permit App",
            "type": "pwa-msedge",
            "request": "launch",
            "url": "http://localhost:5173",
            "webRoot": "${workspaceFolder}"
        }
    ]
}